# Need of Build System

When we are compiling a project containing multiple .cpp files, initially all the files are compiled independently
from each other 
















They generate N different compiled binaries.